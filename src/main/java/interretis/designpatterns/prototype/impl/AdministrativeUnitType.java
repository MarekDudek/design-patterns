package interretis.designpatterns.prototype.impl;

import interretis.designpatterns.prototype.api.PrototypeType;

public enum AdministrativeUnitType implements PrototypeType {

    COUNTRY,
    PROVINCE,
    DISTRICT,
    COMMUNE
}
